# Prototype API for dashboards
This is just a prototype for a dashboard api using aws lambda and the serverless framework. Not under active development

## Tests
With npm installed
* `npm install && cd tests && npm run tests`